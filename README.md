# Python Qlib Catch-up

This repo is a compact Python refresh and Qlib kickoff workspace.

Start with:

- `pythonNotes.md` for the single study document
- `.vscode/` for editor setup
- `scripts/` for future runnable demos
- `src/catchup/` for future reusable code
- `tests/` for future validation

Large market data and downloaded Qlib datasets should stay out of Git.
